This folder preserves early UI experiments that are currently unused in the production build.
They are kept outside of `src/` so the Next.js compiler ignores them, but future work can
copy them back into the main app if needed.
